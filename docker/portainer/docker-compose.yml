services:
  portainer:
    image: portainer/portainer-ce:lts
    container_name: portainer
    pull_policy: always
    ports:
      - "9000:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    restart: always

volumes:
  portainer_data:
